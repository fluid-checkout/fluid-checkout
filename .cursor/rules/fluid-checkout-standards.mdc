---
description: Fluid Checkout ecosystem coding standards (local symlink to shared repo)
alwaysApply: true
---

# Fluid Checkout standards

All conventions live in `fluid-checkout-standards/.cursor/rules/`. Read the rule for the language you are editing.

## Build output — never edit

- **Source:** `js-src/`, `sass/`, PHP under `inc/` and plugin root.
- **Generated (ignore):** `js/`, `css/`, `languages/`.

Do not read, edit, or suggest changes under `js/`, `css/`, or `languages/`.

// Load all rules from the shared standards rules directory automatically.
// This ensures all Fluid Checkout standards are always up-to-date, 
// and avoids manual updates when new rule files are added.
@fluid-checkout-standards/.cursor/rules/*
